<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.qugengting.email">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@drawable/logo_email"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@android:style/Theme.Light.NoTitleBar">
        <activity
            android:name=".activity.MainActivity"
            android:theme="@style/AppTheme">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.EmailDetailActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".activity.EmailSendEditActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".activity.EmailSendListActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".activity.EmailReplyActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".activity.EmailSearchActivity"
            android:theme="@style/AppTheme" />
    </application>

</manifest>